export const strUniqueReq = {
    type: String,
    unique: true,
    required: true
};

export const strReq = {
    type: String,
    required: true
};

export const str = {
    type: String
};

export const num = {
    type: Number,
};
export const numReq = {
    type: Number,
    required: true
};

export const numUniqReq = {
    type: Number,
    unique: true,
    required: true
};
